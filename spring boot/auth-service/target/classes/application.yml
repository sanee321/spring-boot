server:
  port: 9000

spring:
  application:
    name: auth-service
  security:
    oauth2:
      authorizationserver:
        issuer-uri: http://localhost:9000

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true
